# Production environment variables
# For single-binary deployment, use empty values to use same origin

# Backend API URL (without trailing slash)
# Empty = use same origin as frontend (single binary deployment)
VITE_API_URL=

# WebSocket URL (without trailing slash)
# Empty = use same origin as frontend (single binary deployment)
VITE_WS_URL=
