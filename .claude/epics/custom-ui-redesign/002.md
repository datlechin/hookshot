---
name: Build base UI components
status: open
created: 2026-01-30T04:35:06Z
updated: 2026-01-30T04:35:06Z
github: [Will be updated when synced to GitHub]
depends_on: [001]
parallel: true
conflicts_with: []
---

# Task: Build Base UI Components

## Description

Create the foundational UI components (Button, Badge, Toast) using CSS Modules. These are simple, self-contained components that don't have complex interactions and serve as building blocks for other components.

## Acceptance Criteria

- [x] Button component with 4 variants (primary, secondary, ghost, danger)
- [x] Button component with 3 sizes (sm, md, lg)
- [x] Badge component for status indicators (method colors)
- [x] Toast notification component with success/error/info variants
- [x] All components use CSS Modules for styling
- [x] All components use design tokens from Task 001
- [x] Components are keyboard accessible
- [x] TypeScript props interfaces defined

## Technical Details

### Components to Build

**1. Button Component**

File: `frontend/src/components/ui/Button/Button.tsx`
```tsx
import styles from './Button.module.css'

interface ButtonProps extends React.ButtonHTMLAttributes<HTMLButtonElement> {
  variant?: 'primary' | 'secondary' | 'ghost' | 'danger'
  size?: 'sm' | 'md' | 'lg'
  children: React.ReactNode
}

export function Button({
  variant = 'primary',
  size = 'md',
  children,
  className = '',
  ...props
}: ButtonProps) {
  return (
    <button
      className={`${styles.btn} ${styles[variant]} ${styles[size]} ${className}`}
      {...props}
    >
      {children}
    </button>
  )
}
```

File: `frontend/src/components/ui/Button/Button.module.css`
```css
.btn {
  font-family: var(--font-mono);
  font-weight: 500;
  border: 1px solid var(--border);
  border-radius: var(--radius-sm);
  cursor: pointer;
  transition: all var(--transition-fast);
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: var(--space-sm);
}

.btn:hover:not(:disabled) {
  transform: translateY(-1px);
  box-shadow: var(--shadow-sm);
}

.btn:active:not(:disabled) {
  transform: translateY(0);
}

.btn:disabled {
  opacity: 0.5;
  cursor: not-allowed;
}

/* Variants */
.primary {
  background: var(--accent);
  color: white;
  border-color: var(--accent);
}

.secondary {
  background: transparent;
  color: var(--text-primary);
  border-color: var(--border);
}

.ghost {
  background: transparent;
  color: var(--text-secondary);
  border-color: transparent;
}

.danger {
  background: var(--error);
  color: white;
  border-color: var(--error);
}

/* Sizes */
.sm {
  padding: var(--space-sm) var(--space-md);
  font-size: var(--text-sm);
}

.md {
  padding: var(--space-md) var(--space-xl);
  font-size: var(--text-base);
}

.lg {
  padding: var(--space-lg) var(--space-2xl);
  font-size: var(--text-lg);
}
```

**2. Badge Component**

File: `frontend/src/components/ui/Badge/Badge.tsx`
```tsx
import styles from './Badge.module.css'

interface BadgeProps {
  variant?: 'default' | 'success' | 'warning' | 'error' | 'info'
  children: React.ReactNode
  className?: string
}

export function Badge({ variant = 'default', children, className = '' }: BadgeProps) {
  return (
    <span className={`${styles.badge} ${styles[variant]} ${className}`}>
      {children}
    </span>
  )
}
```

File: `frontend/src/components/ui/Badge/Badge.module.css`
```css
.badge {
  display: inline-flex;
  align-items: center;
  padding: 2px var(--space-sm);
  font-size: var(--text-xs);
  font-weight: 500;
  border-radius: var(--radius-sm);
  line-height: 1;
}

.default {
  background: var(--bg-secondary);
  color: var(--text-secondary);
}

.success {
  background: rgba(63, 185, 80, 0.1);
  color: var(--success);
  border: 1px solid var(--success);
}

.warning {
  background: rgba(210, 153, 34, 0.1);
  color: var(--warning);
  border: 1px solid var(--warning);
}

.error {
  background: rgba(248, 81, 73, 0.1);
  color: var(--error);
  border: 1px solid var(--error);
}

.info {
  background: rgba(121, 192, 255, 0.1);
  color: var(--info);
  border: 1px solid var(--info);
}
```

**3. Toast Component**

File: `frontend/src/components/ui/Toast/Toast.tsx`
```tsx
import { useEffect } from 'react'
import styles from './Toast.module.css'

interface ToastProps {
  message: string
  variant?: 'success' | 'error' | 'info'
  onClose: () => void
  duration?: number
}

export function Toast({ message, variant = 'info', onClose, duration = 2000 }: ToastProps) {
  useEffect(() => {
    const timer = setTimeout(onClose, duration)
    return () => clearTimeout(timer)
  }, [duration, onClose])

  return (
    <div className={`${styles.toast} ${styles[variant]}`}>
      <span>{message}</span>
    </div>
  )
}

// Toast container for stacking multiple toasts
export function ToastContainer({ children }: { children: React.ReactNode }) {
  return <div className={styles.container}>{children}</div>
}
```

File: `frontend/src/components/ui/Toast/Toast.module.css`
```css
.container {
  position: fixed;
  bottom: var(--space-2xl);
  right: var(--space-2xl);
  z-index: 9999;
  display: flex;
  flex-direction: column;
  gap: var(--space-md);
}

.toast {
  padding: var(--space-md) var(--space-xl);
  border-radius: var(--radius-md);
  font-size: var(--text-sm);
  font-weight: 500;
  box-shadow: var(--shadow-lg);
  animation: slideIn var(--transition-base);
  min-width: 250px;
}

@keyframes slideIn {
  from {
    transform: translateX(100%);
    opacity: 0;
  }
  to {
    transform: translateX(0);
    opacity: 1;
  }
}

.success {
  background: var(--success);
  color: white;
}

.error {
  background: var(--error);
  color: white;
}

.info {
  background: var(--info);
  color: white;
}
```

**4. Export Barrel**

File: `frontend/src/components/ui/index.ts`
```tsx
export { Button } from './Button/Button'
export { Badge } from './Badge/Badge'
export { Toast, ToastContainer } from './Toast/Toast'
```

## Implementation Steps

1. Create `frontend/src/components/ui/` directory structure
2. Build Button component (tsx + module.css)
3. Build Badge component (tsx + module.css)
4. Build Toast component (tsx + module.css)
5. Create barrel export file
6. Test each component in isolation (create temp test page)
7. Verify accessibility (keyboard navigation, focus states)

## Dependencies

- [x] Task 001 complete (design tokens available)
- [ ] CSS Modules support in Vite (already configured)

## Effort Estimate

- **Size**: M
- **Hours**: 6-8 hours
- **Parallel**: ✅ Yes - Can run after Task 001

## Definition of Done

- [x] All 3 components built and exported
- [x] CSS Modules working correctly
- [x] Design tokens used throughout
- [x] Keyboard accessible (Tab, Enter, Esc)
- [x] TypeScript types defined
- [x] Components tested in browser
- [x] No console errors or warnings
- [x] Committed to version control
